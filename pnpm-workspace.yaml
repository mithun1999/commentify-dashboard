# pnpm refuses to run a package's install scripts until it is listed here.
# @swc/core and esbuild fetch their platform binary in one; core-js only
# prints a funding notice.
allowBuilds:
  '@swc/core': true
  core-js: true
  esbuild: true
